/*! CF v1.0.0 | Compendium Framework |  */
$(function () {
    const MESSAGES_COOKIE_NAME = 'cf-messages';
    let messagesCookieValue = null;
    if (Cookies && (messagesCookieValue = Cookies.get(MESSAGES_COOKIE_NAME))) {
        debugger;
    }
});